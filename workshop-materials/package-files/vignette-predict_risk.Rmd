---
title: "Predict risk of Leukemia remission"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Predict risk of Leukemia remission}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

### Predictions with survival model

#### Single input 

```{r}
library(rcctvm)
predict_risk(sex = "female", wbc = 2.1, treatment = "new")
predict_risk(sex = "male", wbc = 4.3, treatment = "standard")
```

#### Vector input 

```{r}
predict_risk(sex = c("female", "male"),  wbc = c(2.1, 4.3), 
             treatment = c("new", "standard"))
```

#### Data frame input
```{r}
testdata <- data.frame(
  sex = c("female", "male"), 
  wbc = c(2.1, 4.3),
  treatment = c("new", "standard"))

predict_risk(x = testdata)
```
